name: ci

# Every push and pull request runs the test jobs. A version tag (v0.1.0)
# additionally builds the Python package and, only if every test job
# passed, publishes it to PyPI through trusted publishing: PyPI is told
# once that this repository's ci.yml, environment "pypi", may upload
# the project "regtable"; no token is stored anywhere.
on:
  push:
  pull_request:

jobs:
  # -Werror build + regression test on the three desktop platforms
  strict:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            cc: gcc
          - os: ubuntu-latest
            cc: clang
          - os: macos-latest
            cc: clang
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: make strict CC=${{ matrix.cc }}

  # Windows cmd path: build.bat, no make
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - shell: cmd
        run: |
          .\build strict
      # the codegen pipeline on Windows: CreateProcess paths, pipes, GBK console
      - shell: cmd
        run: |
          pip install pyyaml
          python python\tests\gen_test.py || exit /b 1
          python python\regtable gen tools\example.yaml -o gen || exit /b 1
          gcc -std=c99 -Wall -Wextra -Wpedantic -Werror -O2 -Isrc -Igen -o gen\cli.exe tools\gen_cli.c gen\registers.c src\regtable_core.c src\regtable_cli.c src\regtable_modbus.c src\regtable_mqtt.c || exit /b 1
          python python\tests\client_test.py gen\cli.exe example.exe gen || exit /b 1
          python python\regtable watch --yaml tools\example.yaml --every 0 --count 1 --json --pipe gen\cli.exe || exit /b 1
          python python\regtable watch temp led --every 0 --count 1 --pipe gen\cli.exe || exit /b 1
          pip install . || exit /b 1
          regtable --version || exit /b 1

  # ILP32: long is 32 bits, so the ULONG_MAX guards compile out and
  # the parser must hold on its own (as it will on the MCU)
  x86-32bit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y gcc-multilib
      - run: make strict CFLAGS="-std=c99 -Wall -Wextra -Wpedantic -O2 -m32"

  # the library sources under the real target toolchain, compile only
  cortex-m:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y gcc-arm-none-eabi
      - run: |
          for cpu in cortex-m0 cortex-m4; do
            arm-none-eabi-gcc -std=c99 -Wall -Wextra -Wpedantic -Werror -Os \
              -mcpu=$cpu -mthumb -ffunction-sections -fdata-sections \
              -Isrc -c src/regtable_core.c src/regtable_cli.c src/regtable_modbus.c src/regtable_mqtt.c
            arm-none-eabi-size regtable_core.o regtable_cli.o regtable_modbus.o regtable_mqtt.o
          done
      # a table with silicon registers picked from an SVD, on the target toolchain
      - run: pip install pyyaml
      - run: |
          python python/regtable gen python/tests/svd_demo.yaml -o gen_svd
          arm-none-eabi-gcc -std=c99 -Wall -Wextra -Wpedantic -Werror -Os \
            -mcpu=cortex-m0plus -mthumb -Isrc -Igen_svd -c gen_svd/registers.c -o gen_svd/registers.o
          arm-none-eabi-size gen_svd/registers.o

  # the Arduino example, built the way the IDE builds it: repo as a
  # library (src/ + library.properties), sketch is C++, core is avr-gcc
  arduino:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: arduino/setup-arduino-cli@v2
      - run: arduino-cli core update-index && arduino-cli core install arduino:avr
      - run: arduino-cli lib install Ethernet PubSubClient
      - run: arduino-cli compile --fqbn arduino:avr:uno --library . --warnings all examples/arduino
      - run: arduino-cli compile --fqbn arduino:avr:uno --library . --warnings all examples/arduino_modbus
      - run: arduino-cli compile --fqbn arduino:avr:uno --library . --warnings all examples/arduino_modbus_tcp
      - run: arduino-cli compile --fqbn arduino:avr:uno --library . --warnings all examples/arduino_mqtt

  # runtime checks: memory errors and undefined behaviour under the test
  sanitizers:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          gcc -std=c99 -Wall -Wextra -Wpedantic -Werror -O1 -g \
            -fsanitize=address,undefined -fno-sanitize-recover=all \
            -Isrc -o regtest regtable_test.c src/regtable_core.c src/regtable_cli.c -lm
          ./regtest
          gcc -std=c99 -Wall -Wextra -Wpedantic -Werror -O1 -g \
            -fsanitize=address,undefined -fno-sanitize-recover=all \
            -Isrc -o mbtest regtable_modbus_test.c src/regtable_core.c src/regtable_cli.c src/regtable_modbus.c -lm
          ./mbtest
          gcc -std=c99 -Wall -Wextra -Wpedantic -Werror -O1 -g \
            -fsanitize=address,undefined -fno-sanitize-recover=all \
            -Isrc -o mqtest regtable_mqtt_test.c src/regtable_core.c src/regtable_cli.c src/regtable_modbus.c src/regtable_mqtt.c -lm
          ./mqtest

  # YAML -> C table -> compile -> run, plus the validator rejections,
  # then the pip package: build, install, drive the installed command
  codegen:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pip install pyyaml
      - run: make codegen
      - run: pip install .
      - run: |
          cd /tmp
          regtable --version
          regtable gen "$GITHUB_WORKSPACE/tools/example.yaml" -o pkgout
          test -f pkgout/demo_client.py && test -f pkgout/regtable_client.py
          regtable gen "$GITHUB_WORKSPACE/python/tests/svd_demo.yaml" -o pkgsvd
          grep -q "volatile void \*)0x4000441CU" pkgsvd/registers.c
          regtable watch --yaml "$GITHUB_WORKSPACE/tools/example.yaml" --every 0 --count 1 --pipe "$GITHUB_WORKSPACE/gen/cli"
          regtable watch temp led --every 0 --count 1 --pipe "$GITHUB_WORKSPACE/gen/cli"
          python -c "import regtable.panel as p; print(p.panel_path())"

  # coverage-guided search on the CLI byte path
  fuzz:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make fuzz FUZZ_TIME=90
      - run: make fuzz-mb FUZZ_TIME=90
      - if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: fuzz-crashes
          path: |
            crash-*
            leak-*
            timeout-*
          if-no-files-found: ignore

  # on a version tag: build the package once every test job is green,
  # and refuse a tag that does not match __version__
  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [strict, windows, x86-32bit, cortex-m, arduino, sanitizers, codegen, fuzz]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install build
      - run: python -m build
      - run: |
          v=$(python -c "import re,pathlib; print(re.search(r'__version__ = \"([^\"]+)\"', pathlib.Path('python/regtable/__init__.py').read_text()).group(1))")
          test "v$v" = "${GITHUB_REF_NAME}" || { echo "tag ${GITHUB_REF_NAME} != __version__ $v"; exit 1; }
      - uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/

  pypi:
    needs: release
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
